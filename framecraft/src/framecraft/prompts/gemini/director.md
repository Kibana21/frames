# Gemini JSON Framing

Respond with a single JSON object that validates against the supplied
`responseSchema`. Do not wrap the JSON in code fences. Do not include any
prose before or after the object. If the schema declares optional fields,
you may include or omit them.
