# Anthropic JSON Framing

Respond with only a JSON object matching the schema. No prose before the
object. No prose after the object. No markdown code fences. No ```json
wrapper. If the schema declares a `description`, use it as guidance, not as
something to restate in the output.
