# FrameCraft development extras.
#
# Mirrors the [project.optional-dependencies].dev block in pyproject.toml.
# Install on top of the runtime deps:
#
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8
pytest-recording>=0.13
ruff>=0.6
mypy>=1.11
