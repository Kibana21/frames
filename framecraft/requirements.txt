# FrameCraft runtime dependencies.
#
# Mirrors the [project.dependencies] block in pyproject.toml.
# Keep the two in sync when adding/bumping packages.
#
# After `pip install -r requirements.txt`, run `pip install -e .` to register
# the `framecraft` CLI entry point.

pydantic>=2.6
jinja2>=3.1
typer[all]>=0.12
rich>=13
httpx>=0.27
beautifulsoup4>=4.12
pyyaml>=6
platformdirs>=4

# LLM provider SDKs — the adapter layer imports them lazily, so a missing
# SDK only surfaces when its provider is actually selected at runtime.
google-genai>=0.3
anthropic>=0.39
